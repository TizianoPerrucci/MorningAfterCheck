The Morning After Check is a sample application based on an abstraction of the drinking domain.

Launch SBT:
$ sbt

Run Jetty (application running on http://localhost:8080/):
sbt> container:start

Keep recompiling:
sbt> ~ compile

------------------------------------------------------------------------------------------------

This application is configured to run on Heroku.